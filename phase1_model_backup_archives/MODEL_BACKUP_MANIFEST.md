# Phase 1 model artifact backup

This backup contains only the current model-related artifacts from:

```text
E:\申博材料\Davood Shojaei\cache\model_phase1
```

The legacy model runner below is included in `part_01.zip`:

```text
E:\申博材料\Davood Shojaei\deepseek_python_20260825_500d35.py
```

## Archive layout

The archive parts preserve the intended repository path inside each ZIP:

```text
phase1/cache/model_phase1/...
phase1/deepseek_python_20260825_500d35.py
```

To restore, extract all `part_*.zip` files into the same directory and keep the `phase1/` prefix.

## Exclusions

- `deepseek_env_20260825_d2515f.env` — excluded because it contains an API key.
- All `*.pyc` files — generated bytecode/cache files.
- All `*.safetensors` files — local embedding model weights; the model can be downloaded and reconstructed from the retained configuration and index metadata.
- CV, research-proposal, application, and other non-model project materials.

The archive parts are split for GitHub web-upload compatibility. The source files on the local `E:` drive were copied and compressed; they were not moved or deleted.
