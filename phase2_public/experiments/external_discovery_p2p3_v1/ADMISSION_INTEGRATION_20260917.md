# Scoped admission integration checkpoint

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: run
- Origin Date: 2026-09-17
- Verification Status: UNVERIFIED
- Version Label: p3_scoped_admission_v1

This is a software/admission checkpoint, not a legal-effectiveness benchmark.

An explicit operator approval for 30 tasks and eight bound article snapshots has been materialized in private records. The initial public pending-task file and historical startup results are preserved. No approval is inferred from the presence of a credential or generated by the runtime itself.

## Executed checks

- Eight exact article snapshots passed in-scope admission checks.
- Seven excluded-context mutations per article were blocked: unregistered issue, future date, historical date, missing scope, changed task specification, wrong regime and missing location. **56/56 negative gate checks passed.**
- The experiment test suite now has 56 passing tests, including 11 newly added scope/approval tests. These software unit tests are separate from the 64 checks above; do not treat their sum as an independent legal sample size.
- Unknown statutory commencement/expiry dates were left null. The scope uses the registered case date only, not an invented period of legal validity.
- Reference labels are held separately from runtime approvals; a library entry still requires actual per-case applicability checks. No reference ID is used to force a retrieval hit.
- No new model requests, external page requests or expert-data access were required for this checkpoint.

## Remaining execution work

Formal A/B/C evaluation is not yet executed. It still requires fact-only inputs (without expected conclusions or test instructions), validated execution/fault-injection adapters, matched corpus/withholding and request parameters, and citation traceability through the final export. No new Recall@5, MRR, precision, supported-rescue rate or end-to-end efficiency gain is claimed.

The original expert-rated model, prompt, corpus and outputs are unchanged. Only generic model integration code, tests and this aggregate checkpoint belong in the public repository. All actual approval records, labels and source snapshots remain private.
